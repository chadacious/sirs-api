// import { log } from '@medlor/medlor-auth-token-lib';

export default (userId, entity, source) => {
    // log.trace(`Checking permission for: ${userId} to upsert: ${entity}: ${source}`);
    return true;
};
